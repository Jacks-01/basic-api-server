'use strict';

const express = require('express');
const HatModel = require('../models/');
const router = require('./cats');


const Router = express.Router;


router.get('/hats', (req, res, next) => {

});